# Optional arguments passed to `zola proxy start` by the systemd service.
#
# Example:
# ZOLA_PROXY_ARGS="--provider deepseek"
ZOLA_PROXY_ARGS=""

# User that owns the Codex/Zola configuration. If empty, the installer
# detects SUDO_USER, PKEXEC_UID, or falls back to root.
ZOLA_SERVICE_USER=""
